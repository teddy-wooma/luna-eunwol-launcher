#version 150
#define HEIGHT_BIT 13
#define MAX_BIT 10
#define ADD_OFFSET 4095
#define DISPLAY_HEIGHT 8192.0 / 40.0
#define DEFAULT_OFFSET 10
#define SHADER_VERSION 2
#moj_import <fog.glsl>
#if SHADER_VERSION >= 3
#moj_import <dynamictransforms.glsl>
#moj_import <projection.glsl>
#moj_import <globals.glsl>
out float sphericalVertexDistance;out float cylindricalVertexDistance;
#else
uniform mat4 ProjMat;uniform mat4 ModelViewMat;uniform int FogShape;out float vertexDistance;uniform vec2 ScreenSize;uniform float GameTime;
#endif
in vec3 Position;in vec4 Color;in vec2 UV0;in ivec2 UV2;uniform sampler2D Sampler0;uniform sampler2D Sampler2;uniform vec3 ChunkOffset;out vec4 vertexColor;out vec2 texCoord0;out float applyColor;bool range(float t, float m1, float m2) {return t >= m1 && t <= m2;}bool range(vec2 t, vec2 m1, vec2 m2) {return range(t.x, m1.x, m2.x) && range(t.y, m1.y, m2.y);}bool range(vec3 t, vec3 m1, vec3 m2) {return range(t.x, m1.x, m2.x) && range(t.y, m1.y, m2.y) && range(t.z, m1.z, m2.z);}bool checkElement(float z) {if (z == 0) return true; else if (z == 1000) return true; else if (z == -90) return true; else if (z == 2800) return true; return false;}float fogDistance(vec3 pos, int shape) {if (shape == 0) {return length(pos);} else {float distXZ = length(pos.xz);float distY = abs(pos.y);return max(distXZ, distY);}}vec3 betterhealthbar_rotate(vec3 v, vec4 q) {vec3 u = q.xyz;float s = q.w;return 2.0 * dot(u, v) * u + (s * s - dot(u, u)) * v + 2.0 * s * cross(u, v);}vec4 betterhealthbar_toQuaternion(float roll, float pitch, float yaw) {float cr = cos(roll * 0.5);float sr = sin(roll * 0.5);float cp = cos(pitch * 0.5);float sp = sin(pitch * 0.5);float cy = cos(yaw * 0.5);float sy = sin(yaw * 0.5);return vec4(sr * cp * cy - cr * sp * sy, cr * sp * cy + sr * cp * sy, cr * cp * sy - sr * sp * cy, cr * cp * cy + sr * sp * sy);}void main() {vec3 pos = Position;vec2 ui = ceil(2 / vec2(ProjMat[0][0], -ProjMat[1][1]));vec2 uiScreen = ui / ScreenSize;vec3 color = Color.xyz;applyColor = 0;vertexColor = Color * texelFetch(Sampler2, UV2 / 16, 0);if (pos.y >= ui.y && ProjMat[3].x == -1) {int bit = int(pos.y) >> HEIGHT_BIT;if (((bit >> MAX_BIT) & 1) == 1) {int id = bit - (1 << MAX_BIT);pos.x -= 0.5 * ui.x;pos.y -= (bit << HEIGHT_BIT) + ADD_OFFSET + DEFAULT_OFFSET;float xGui = 0;float yGui = 0;float layer = 0;float opacity = 1;bool outline = false;int property = 0;switch (id) {case 1:break;case 2:layer = 1;outline = true;break;case 3:layer = 2;break;case 4:layer = 3;break;case 5:layer = 4;break;case 6:layer = 100;outline = true;break;case 7:layer = 100;break;case 8:yGui = ui.y * 100.0 / 100.0;break;case 9:yGui = ui.y * 100.0 / 100.0;layer = 100;outline = true;break;case 10:xGui = ui.x * 50.0 / 100.0;outline = true;break;case 11:xGui = ui.x * 50.0 / 100.0;break;case 12:opacity = 0.0;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 13:opacity = 0.1;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 14:opacity = 0.1;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 15:opacity = 0.2;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 16:opacity = 0.2;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 17:opacity = 0.3;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 18:opacity = 0.3;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 19:opacity = 0.4;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 20:opacity = 0.4;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 21:opacity = 0.5;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 22:opacity = 0.6;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 23:opacity = 0.6;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 24:opacity = 0.7;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 25:opacity = 0.7;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 26:opacity = 0.8;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 27:opacity = 0.9;pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 28:pos.x = (pos.x - (0)) * 7.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 7.6 * uiScreen.y + (0);xGui = ui.x * 50.0 / 100.0;layer = 99;break;case 29:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 50.0 / 100.0;layer = 1;break;case 30:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 50.0 / 100.0;layer = 2;break;case 31:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = -3;break;case 32:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;outline = true;break;case 33:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;break;case 34:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 1;outline = true;break;case 35:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 1;break;case 36:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 2;outline = true;break;case 37:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 2;outline = true;break;case 38:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 2;break;case 39:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 3;break;case 40:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 10;outline = true;break;case 41:xGui = ui.x * 50.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 100;break;case 42:opacity = 0.0;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 43:opacity = 0.1;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 44:opacity = 0.1;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 45:opacity = 0.2;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 46:opacity = 0.2;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 47:opacity = 0.3;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 48:opacity = 0.3;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 49:opacity = 0.4;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 50:opacity = 0.4;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 51:opacity = 0.5;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 52:opacity = 0.6;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 53:opacity = 0.6;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 54:opacity = 0.7;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 55:opacity = 0.7;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 56:opacity = 0.8;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 57:opacity = 0.9;pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 58:pos.x = (pos.x - (0)) * 3.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.0 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 30.0 / 100.0;layer = 100;outline = true;break;case 59:opacity = 0.0;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 60:opacity = 0.1;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 61:opacity = 0.1;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 62:opacity = 0.2;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 63:opacity = 0.2;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 64:opacity = 0.3;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 65:opacity = 0.3;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 66:opacity = 0.4;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 67:opacity = 0.4;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 68:opacity = 0.5;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 69:opacity = 0.6;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 70:opacity = 0.6;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 71:opacity = 0.7;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 72:opacity = 0.7;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 73:opacity = 0.8;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 74:opacity = 0.9;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 75:pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 40.0 / 100.0;layer = 100;outline = true;break;case 76:opacity = 0.0;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 77:opacity = 0.1;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 78:opacity = 0.1;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 79:opacity = 0.2;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 80:opacity = 0.2;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 81:opacity = 0.3;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 82:opacity = 0.3;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 83:opacity = 0.4;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 84:opacity = 0.4;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 85:opacity = 0.5;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 86:opacity = 0.6;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 87:opacity = 0.6;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 88:opacity = 0.7;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 89:opacity = 0.7;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 90:opacity = 0.8;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 91:opacity = 0.9;pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 92:pos.x = (pos.x - (0)) * 3.6 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 3.6 * uiScreen.y + (0);xGui = ui.x * 70.0 / 100.0;yGui = ui.y * 43.5 / 100.0;layer = 100;outline = true;break;case 93:opacity = 0.0;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 94:opacity = 0.1;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 95:opacity = 0.2;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 96:opacity = 0.3;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 97:opacity = 0.4;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 98:opacity = 0.5;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 99:opacity = 0.6;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 100:opacity = 0.7;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 101:opacity = 0.8;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 102:opacity = 0.9;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 103:pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 104:opacity = 0.9;pos.x = (pos.x - (2)) * 1.8 * uiScreen.x + (2);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 105:opacity = 0.8;pos.x = (pos.x - (4)) * 1.8 * uiScreen.x + (4);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 106:opacity = 0.7;pos.x = (pos.x - (6)) * 1.8 * uiScreen.x + (6);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 107:opacity = 0.6;pos.x = (pos.x - (8)) * 1.8 * uiScreen.x + (8);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 108:opacity = 0.5;pos.x = (pos.x - (10)) * 1.8 * uiScreen.x + (10);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 109:opacity = 0.4;pos.x = (pos.x - (12)) * 1.8 * uiScreen.x + (12);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 110:opacity = 0.3;pos.x = (pos.x - (14)) * 1.8 * uiScreen.x + (14);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 111:opacity = 0.2;pos.x = (pos.x - (16)) * 1.8 * uiScreen.x + (16);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 112:opacity = 0.1;pos.x = (pos.x - (18)) * 1.8 * uiScreen.x + (18);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 113:opacity = 0.0;pos.x = (pos.x - (20)) * 1.8 * uiScreen.x + (20);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 70.5 / 100.0;yGui = ui.y * 55.2 / 100.0;layer = 100;outline = true;break;case 114:opacity = 0.0;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 115:opacity = 0.1;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 116:opacity = 0.2;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 117:opacity = 0.3;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 118:opacity = 0.4;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 119:opacity = 0.5;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 120:opacity = 0.6;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 121:opacity = 0.7;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 122:opacity = 0.8;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 123:opacity = 0.9;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 124:pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 125:opacity = 0.9;pos.x = (pos.x - (2)) * 1.8 * uiScreen.x + (2);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 126:opacity = 0.8;pos.x = (pos.x - (4)) * 1.8 * uiScreen.x + (4);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 127:opacity = 0.7;pos.x = (pos.x - (6)) * 1.8 * uiScreen.x + (6);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 128:opacity = 0.6;pos.x = (pos.x - (8)) * 1.8 * uiScreen.x + (8);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 129:opacity = 0.5;pos.x = (pos.x - (10)) * 1.8 * uiScreen.x + (10);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 130:opacity = 0.4;pos.x = (pos.x - (12)) * 1.8 * uiScreen.x + (12);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 131:opacity = 0.3;pos.x = (pos.x - (14)) * 1.8 * uiScreen.x + (14);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 132:opacity = 0.2;pos.x = (pos.x - (16)) * 1.8 * uiScreen.x + (16);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 133:opacity = 0.1;pos.x = (pos.x - (18)) * 1.8 * uiScreen.x + (18);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 134:opacity = 0.0;pos.x = (pos.x - (20)) * 1.8 * uiScreen.x + (20);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 72.5 / 100.0;yGui = ui.y * 60.5 / 100.0;layer = 100;outline = true;break;case 135:opacity = 0.0;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 136:opacity = 0.1;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 137:opacity = 0.1;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 138:opacity = 0.2;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 139:opacity = 0.2;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 140:opacity = 0.3;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 141:opacity = 0.3;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 142:opacity = 0.4;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 143:opacity = 0.4;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 144:opacity = 0.5;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 145:opacity = 0.6;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 146:opacity = 0.6;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 147:opacity = 0.7;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 148:opacity = 0.7;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 149:opacity = 0.8;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 150:opacity = 0.9;pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 151:pos.x = (pos.x - (0)) * 2.0 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 2.0 * uiScreen.y + (0);xGui = ui.x * 80.0 / 100.0;yGui = ui.y * 55.0 / 100.0;layer = 99;break;case 152:opacity = 0.0;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 153:opacity = 0.1;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 154:opacity = 0.2;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 155:opacity = 0.3;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 156:opacity = 0.4;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 157:opacity = 0.5;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 158:opacity = 0.6;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 159:opacity = 0.7;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 160:opacity = 0.8;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 161:opacity = 0.9;pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 162:pos.x = (pos.x - (0)) * 1.8 * uiScreen.x + (0);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 163:opacity = 0.9;pos.x = (pos.x - (2)) * 1.8 * uiScreen.x + (2);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 164:opacity = 0.8;pos.x = (pos.x - (4)) * 1.8 * uiScreen.x + (4);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 165:opacity = 0.7;pos.x = (pos.x - (6)) * 1.8 * uiScreen.x + (6);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 166:opacity = 0.6;pos.x = (pos.x - (8)) * 1.8 * uiScreen.x + (8);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 167:opacity = 0.5;pos.x = (pos.x - (10)) * 1.8 * uiScreen.x + (10);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 168:opacity = 0.4;pos.x = (pos.x - (12)) * 1.8 * uiScreen.x + (12);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 169:opacity = 0.3;pos.x = (pos.x - (14)) * 1.8 * uiScreen.x + (14);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 170:opacity = 0.2;pos.x = (pos.x - (16)) * 1.8 * uiScreen.x + (16);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 171:opacity = 0.1;pos.x = (pos.x - (18)) * 1.8 * uiScreen.x + (18);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 172:opacity = 0.0;pos.x = (pos.x - (20)) * 1.8 * uiScreen.x + (20);pos.y = (pos.y - (0)) * 1.8 * uiScreen.y + (0);xGui = ui.x * 90.0 / 100.0;yGui = ui.y * 60.0 / 100.0;layer = 100;outline = true;break;case 173:xGui = ui.x * 100.0 / 100.0;layer = 2;break;case 174:xGui = ui.x * 100.0 / 100.0;layer = 3;break;case 175:xGui = ui.x * 100.0 / 100.0;layer = 4;break;case 176:xGui = ui.x * 100.0 / 100.0;yGui = ui.y * 100.0 / 100.0;break;case 177:xGui = ui.x * 100.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 1;break;case 178:pos.x = (pos.x - (-52)) * 0.7 + (-52);pos.y = (pos.y - (-29)) * 0.7 + (-29);xGui = ui.x * 100.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 1;break;case 179:pos.x = (pos.x - (-52)) * 0.7 + (-52);pos.y = (pos.y - (-20)) * 0.7 + (-20);xGui = ui.x * 100.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 1;break;case 180:pos.x = (pos.x - (-52)) * 0.7 + (-52);pos.y = (pos.y - (-11)) * 0.7 + (-11);xGui = ui.x * 100.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 1;break;case 181:xGui = ui.x * 100.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 2;outline = true;break;case 182:xGui = ui.x * 100.0 / 100.0;yGui = ui.y * 100.0 / 100.0;layer = 2;break;}
#if SHADER_VERSION < 2
vertexColor = (checkElement(pos.z) && !outline) ? vec4(0) : Color * texelFetch(Sampler2, UV2 / 16, 0) * vec4(1, 1, 1, opacity);
#else
vertexColor = Color * texelFetch(Sampler2, UV2 / 16, 0) * vec4(1, 1, 1, opacity);
#endif
if ((property & 1) > 0) {pos.y += 4 * sin((GameTime * 1200 + pos.x / ui.x) * 3.1415 * 2);}if ((property & 2) > 0) {int hash = int(pos.x) * int(pos.y);float time = GameTime * 1200;hash = 31 * (hash + int(vertexColor.x + time));float r = float(hash % 224 + 32) / 255;hash = 31 * (hash + int(vertexColor.y + time));float g = float(hash % 224 + 32) / 255;hash = 31 * (hash + int(vertexColor.z + time));float b = float(hash % 224 + 32) / 255;float maxValue = max(max(r, g), b);vertexColor = vec4(pow(r / maxValue, 3), pow(g / maxValue, 3), pow(b / maxValue, 3), vertexColor.w);}if ((property & 4) > 0) {int hash = int(pos.x) * int(pos.y);float time = GameTime * 1200;hash = 31 * (hash + int(vertexColor.x + time));float r = vertexColor.x + float(hash % 64) / 255;hash = 31 * (hash + int(vertexColor.y + time));float g = vertexColor.y + float(hash % 64) / 255;hash = 31 * (hash + int(vertexColor.z + time));float b = vertexColor.z + float(hash % 64) / 255;vertexColor = vec4(r, g, b, vertexColor.w);}pos.x += xGui;pos.y += yGui;pos.z += layer;}} else {}if (ProjMat[3].x != -1) {vec4 texColor = texture(Sampler0, UV0);mat4 invModelViewMat = inverse(ModelViewMat);vec3 location = normalize(vec3(invModelViewMat[2]));float pitch = asin(-location.y);float yaw;if (location.x == 0.0 && location.z == 0.0) {vec3 right = normalize(vec3(ModelViewMat[0]));yaw = pitch > 0 ? atan(right.y, -right.x) : atan(-right.y, -right.x);} else {yaw = atan(location.x, -location.z);}if (length(Position + ChunkOffset - vec3(invModelViewMat[3])) >= (DISPLAY_HEIGHT / 2)) {float alpha = texColor.a;if (alpha < 1) {applyColor = 1;vec3 add = betterhealthbar_rotate(vec3(0, DISPLAY_HEIGHT, alpha / 40), betterhealthbar_toQuaternion(pitch, yaw, 0.0));pos.x += add.x;pos.y += add.y;pos.z -= add.z;}}}
#if SHADER_VERSION >= 3
sphericalVertexDistance = fog_spherical_distance(pos);cylindricalVertexDistance = fog_cylindrical_distance(pos);
#else
vertexDistance = fogDistance(pos, FogShape);
#endif
texCoord0 = UV0;gl_Position = ProjMat * ModelViewMat * vec4(pos, 1.0);}